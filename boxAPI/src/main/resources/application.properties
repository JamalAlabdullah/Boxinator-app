# Configure Spring Security with Keycloak tokens (will just be different URLs if you go with another IDP)
#spring.security.oauth2.resourceserver.jwt.issuer-uri= http://keycloakboxinator.herokuapp.com/auth/realms/boxinator
#spring.security.oauth2.resourceserver.jwt.jwk-set-uri= http://keycloakboxinator.herokuapp.com/auth/realms/boxinator/protocol/openid-connect/certs


# Datasource configuration
spring.datasource.url= jdbc:postgresql://localhost:5432/boxinatorDb
spring.datasource.username= postgres
spring.datasource.password= postgres

# Hibernate behaviour
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQL95Dialect
spring.jpa.hibernate.ddl-auto=create
#Turn Statistics on
logging.level.org.hibernate.stat= trace
# Enable logging
spring.jpa.show-sql=true

#Swagger config
springdoc.swagger-ui.operations-sorter=method

